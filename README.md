# graphrag-ts
Reference implementation of a chunked GraphRAG engine: markdown-aware build pipeline, community detection (Leiden), vector+keyword retrieval and evidence-grounded answer generation. Bun + TypeScript.
